# Integration Test Configuration for Auto Summarization
llm_model_name: "openai:gpt-4o-mini"
system_prompt: "You are a test assistant. Use the provided tools to generate content."
use_history: true
session_id: "integration_test_auto_summary"
history_trunctor_config:
  mode: "auto"
  truncation_threshold: 100
config_name: "integration_auto_summary_config"
